<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Videoplaybackerandroid" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">@color/white</item>
        <item name="android:textColorHint">#666666</item>
        <item name="android:colorBackground">@color/black</item>
        <item name="colorOnSurface">@color/white</item>
        <item name="colorOnBackground">@color/white</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <style name="Theme.Videoplaybackerandroid" parent="Base.Theme.Videoplaybackerandroid" />

    <style name="Theme.Dashboard" parent="Theme.Videoplaybackerandroid">
        <item name="android:textSize">12sp</item>
    </style>
</resources>